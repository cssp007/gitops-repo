apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: frontend
  namespace: monitoring
spec:
  namespaceSelector:
    matchNames:
      - frontend

  selector:
    matchLabels:
      app: frontend

  endpoints:
    - port: http
      path: /metrics
      interval: 10s
