apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: flask-app
  namespace: monitoring
spec:
  namespaceSelector:
    matchNames:
      - frontend

  selector:
    matchLabels:
      app: flask-app

  endpoints:
    - port: http
      path: /metrics
      interval: 10s
